<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AI Prompt Hub & Business Document Manager</title>
<style>
  :root {
    --bg: #0d0d1a;
    --surface: #13132b;
    --surface2: #1a1a35;
    --accent: #7c3aed;
    --accent2: #a855f7;
    --accent3: #06b6d4;
    --text: #e2e8f0;
    --text-muted: #94a3b8;
    --border: rgba(124,58,237,0.25);
    --card-bg: #16163a;
    --card-hover: #1e1e45;
    --radius: 14px;
    --header-bg: rgba(13, 13, 26, 0.85);
  }

  body { background: var(--bg); color: var(--text); font-family: 'Segoe UI', system-ui, sans-serif; min-height: 100vh; margin: 0; }
  * { box-sizing: border-box; scroll-behavior: smooth; }

  /* ── NAVIGATION ── */
  header {
    background: var(--header-bg); border-bottom: 1px solid var(--border);
    padding: 0 2rem; position: sticky; top: 0; z-index: 1000;
    display: flex; align-items: center; justify-content: space-between;
    height: 70px; backdrop-filter: blur(12px);
  }
  .logo { font-size: 1.5rem; font-weight: 800; background: linear-gradient(90deg, #a855f7, #06b6d4); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
  .header-nav { display: flex; gap: 1rem; }
  .nav-btn { 
    background: transparent; border: 1px solid transparent; color: var(--text-muted); 
    padding: 0.6rem 1.2rem; border-radius: 10px; cursor: pointer; font-weight: 600; transition: 0.3s;
  }
  .nav-btn.active { background: var(--accent); color: white; border-color: var(--accent2); }

  /* ── LAYOUT ── */
  .view { display: none; padding: 3rem 2rem; max-width: 1400px; margin: 0 auto; }
  .view.active { display: block; animation: fadeIn 0.4s ease; }
  @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

  /* ── TEMPLATE GRID ── */
  .template-grid { 
    display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); 
    gap: 2rem; margin-top: 2rem; 
  }
  .file-card { 
    background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius);
    padding: 1.5rem; text-align: center; cursor: pointer; transition: 0.3s;
  }
  .file-card:hover { border-color: var(--accent3); transform: scale(1.03); background: var(--card-hover); }
  .file-icon { font-size: 3.5rem; margin-bottom: 1rem; color: var(--accent2); }
  .file-name { font-weight: 700; font-size: 1rem; margin-bottom: 0.4rem; color: white; }
  .file-tag { font-size: 0.75rem; text-transform: uppercase; color: var(--accent3); letter-spacing: 1px; font-weight: 800; }

  /* ── SIDEBAR EDITOR ── */
  .editor-overlay { 
    display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.9); 
    z-index: 2000; align-items: center; justify-content: flex-end; backdrop-filter: blur(8px);
  }
  .editor-overlay.open { display: flex; }
  .editor-panel { 
    background: var(--surface); width: 100%; max-width: 500px; height: 100%; 
    border-left: 2px solid var(--border); padding: 3rem 2rem; overflow-y: auto;
  }
  .editor-panel h2 { margin-top: 0; color: var(--accent2); }
  .field-group { margin-bottom: 1.5rem; }
  .field-group label { display: block; margin-bottom: 0.5rem; font-size: 0.85rem; color: var(--text-muted); }
  .field-group input { 
    width: 100%; background: var(--bg); border: 1.5px solid var(--border); 
    padding: 0.8rem; border-radius: 8px; color: white; outline: none;
  }
  .field-group input:focus { border-color: var(--accent); }
  .action-row { display: flex; gap: 1rem; margin-top: 2rem; }
  .btn-main { background: var(--accent); color: white; border: none; padding: 1rem 2rem; border-radius: 10px; font-weight: 700; cursor: pointer; flex: 1; }
  .btn-close { background: var(--surface2); color: white; border: 1px solid var(--border); padding: 1rem; border-radius: 10px; cursor: pointer; }

  /* ── PROMPT HUB CARDS ── */
  .prompt-cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1.5rem; }
  .prompt-card { 
    background: var(--card-bg); border: 1px solid var(--border); border-radius: var(--radius);
    padding: 1.5rem; cursor: pointer; transition: 0.3s;
  }
  .prompt-card:hover { border-color: var(--accent2); background: var(--card-hover); }
</style>
</head>
<body>

<header>
  <div class="logo">⚡ AI Prompt Hub</div>
  <nav class="header-nav">
    <button id="nav-prompts" class="nav-btn active" onclick="showView('prompts')">600+ AI Prompts</button>
    <button id="nav-templates" class="nav-btn" onclick="showView('templates')">Office Template Hub</button>
  </nav>
</header>

<div id="view-prompts" class="view active">
  <div style="text-align:center; margin-bottom: 3rem;">
    <h1>Curated AI Tool Library</h1>
    <p>Select a professional category to browse 600+ architected prompts.</p>
  </div>
  <div id="category-container"></div>
</div>

<div id="view-templates" class="view">
  <div style="margin-bottom: 3rem;">
    <h1>Business Template Hub</h1>
    <p>Ready-made professional agreements and forms. Click any document to begin the automated drafting process.</p>
  </div>
  <div class="template-grid" id="office-grid"></div>
</div>

<div class="editor-overlay" id="editorOverlay">
  <div class="editor-panel">
    <button class="btn-close" onclick="closeEditor()">✕ Close</button>
    <h2 id="editTitle">Document Drafter</h2>
    <p id="editDesc" style="color: var(--text-muted); margin-bottom: 2rem;">Fill in the details below to generate your professional document.</p>
    
    <div id="formFields">
      <div class="field-group">
        <label>Effective Date</label>
        <input type="date" placeholder="Select date">
      </div>
      <div class="field-group">
        <label>Company/Party A Name</label>
        <input type="text" placeholder="Enter business name">
      </div>
      <div class="field-group">
        <label>Client/Party B Name</label>
        <input type="text" placeholder="Enter client name">
      </div>
      <div class="field-group">
        <label>Service/Project Details</label>
        <input type="text" placeholder="e.g. Consulting Services, Product Sales">
      </div>
    </div>

    <div class="action-row">
      <button class="btn-main" onclick="alert('Document Generated and Ready for Download!')">Generate & Download PDF</button>
    </div>
  </div>
</div>

<script>
// FULL LIST OF YOUR GOOGLE DRIVE TEMPLATES
const OFFICE_TEMPLATES = [
  { name: "Agreement for Reseller", cat: "Business", icon: "🤝" },
  { name: "Asset Purchase Agreement", cat: "Legal", icon: "🏛️" },
  { name: "Bulk Sale Agreement", cat: "Sales", icon: "📦" },
  { name: "Consultant Agreement", cat: "Consulting", icon: "📋" },
  { name: "Consulting Agreement Ind. Contractor", cat: "Legal", icon: "💼" },
  { name: "Credit Agreement", cat: "Finance", icon: "💳" },
  { name: "Distribution Agreement", cat: "Business", icon: "🚢" },
  { name: "Equipment Purchase Agreement", cat: "Sales", icon: "⚙️" },
  { name: "OEM Agreement", cat: "Legal", icon: "🏭" },
  { name: "Product Supplier Agreement", cat: "Operations", icon: "🏭" },
  { name: "Sale of Goods Contract", cat: "Retail", icon: "🏷️" },
  { name: "Sales Agency Agreement", cat: "Legal", icon: "🖋️" },
  { name: "Sponsorship Agreement", cat: "Marketing", icon: "✨" },
  { name: "Supply Agreement", cat: "Logistics", icon: "🚛" },
  { name: "Commitment Form", cat: "General", icon: "📝" },
  { name: "Compromising Debt Agreement", cat: "Finance", icon: "📉" },
  { name: "Conditional Sale Agreement", cat: "Sales", icon: "📑" }
];

// INJECT OFFICE TEMPLATES INTO GRID
function buildTemplates() {
  const grid = document.getElementById('office-grid');
  grid.innerHTML = OFFICE_TEMPLATES.map(doc => `
    <div class="file-card" onclick="openEditor('${doc.name}')">
      <div class="file-icon">${doc.icon}</div>
      <div class="file-name">${doc.name}</div>
      <div class="file-tag">${doc.cat} Document</div>
    </div>
  `).join('');
}

// NAVIGATION LOGIC
function showView(viewId) {
  document.querySelectorAll('.view').forEach(v => v.classList.remove('active'));
  document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
  document.getElementById('view-' + viewId).classList.add('active');
  document.getElementById('nav-' + viewId).classList.add('active');
}

// EDITOR SIDEBAR LOGIC
function openEditor(name) {
  document.getElementById('editTitle').innerText = name;
  document.getElementById('editorOverlay').classList.add('open');
}

function closeEditor() {
  document.getElementById('editorOverlay').classList.remove('open');
}

// INITIALIZE
window.onload = () => {
  buildTemplates();
  // buildPrompts(); // Logic from previous prompt hub remains compatible here
};
</script>
</body>
</html>